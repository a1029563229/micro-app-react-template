const path = require("path");

module.exports = {
  webpack: (config) => {
    return config;
  },

  devServer: function (configFunction) {
    return function (proxy, allowedHost) {
      return config;
    };
  },
};
